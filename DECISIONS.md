# Decision log

Non-obvious choices in fantasy-bff, one line each. The format and rules are in the monorepo
root's `DECISIONS.md`; choices that span repos go there.

---

[2026-09-11] fantasy-bff: whether the AI projection is served is one answer, `AiProjectionAvailability` (`ai-projection.enabled` AND `security.projection-model-enabled`), enforced at both doors and reported on a new public `GET /api/v1/features` that the web follows — rejected: a field on `/api/v1/versions`, which probes three downstreams per call (10 s read timeouts), so a preset would wait on ESPN's actuator; and folding the two properties into one, which would lose "model off, Who's hot on".
[2026-09-11] fantasy-bff: a shared projection is kept out of search results — `noindex` and no canonical on the crawler preview document, `X-Robots-Tag: noindex` on its card — rather than left indexable as a growth channel; Alexander decided a share is for whoever its author sends the link to, since the share dialog never says the page could appear in search and the title carries the author's username. Taking Googlebot out of nginx's link-crawler map instead was passed over: it would then rely on Google rendering the SPA to read the web's noindex, and bots that do not render would index the bare shell.
[2026-09-11] fantasy-bff: a Paddle checkout attaches the buyer's Paddle customer, found or created by email, and only when the account's email is verified — having the web pass the email to Paddle.js instead was rejected because Paddle does not document combining that with a transaction made on the server, and an unverified address could name someone else's customer and open their billing portal to this account; a lookup that fails opens the checkout without a customer rather than failing the purchase.
[2026-09-11] fantasy-bff: Who's hot's season lengths and default season come from projection-service's `/splits/seasons` (served as `/projection-model/splits/seasons`), and a split with no season is passed on without one, replacing the 82-game constant and "the projected season minus one" (#223, fantasy-projection-service#165) — 2026-27 is 84 games and "minus one" is wrong from opening night on; a free account's explicit window is judged against its own season's length with a service call, taken only for a window of five or fewer, and a constant 84 was rejected because 2025-26's free five (78-82) would then be refused.
[2026-09-11] fantasy-bff: a checkout for an account that already has one it can still pay hands that same checkout back (its Paddle transaction id stored in db-service by compare-and-set) instead of opening another, so two tabs share one transaction that Paddle lets be paid once — refunding a second payment automatically was rejected because the buyer is still charged and the refund waits on Paddle, and keying reuse on the Paddle customer was rejected because accounts with an unverified email have none.
[2026-09-11] fantasy-bff: the four internal API keys, `JWT_SECRET` and `RESEND_API_KEY` have no default and stop startup when blank (#226, #229); a 401 from the startup key check moves readiness to REFUSING_TRAFFIC, which `/actuator/health` (what the container health check curls) now includes, and the Paddle, mock-payment, Google and Facebook secrets keep empty defaults because their features fail closed. Reset/verification links are logged only under `email.log-links`, which only the dev profile sets — "log that sending is disabled" alone was rejected because a deployed instance would then silently send no email, and a 401 on projection-service refusing traffic for the whole bff was accepted since a mismatched key almost always arrives with a deploy, which then rolls back.
[2026-09-15] fantasy-bff: players' pictures sit behind `players.avatars.enabled` (`PLAYER_AVATARS_ENABLED`), off by default rather than set to false per environment, and are stripped in the BFF (player lists, the headshot endpoint, share reads) with no web flag — they are ESPN's photographs, which Disney's terms do not licence for a product with a paid tier, so a deployment that forgets the variable must fail closed; rejected: a web-side flag on `/api/v1/features`, which would leave the endpoint serving the photographs to anyone asking, and dropping the headshot code, since a licensed picture source would reuse the framing and cache.
[2026-09-15] fantasy-bff: `POST /admin/player-ids/remap` takes `to` (default ESPN) and builds the crosswalk from the side being left, gating an apply on that side's players who played, so the pool can move back to Yahoo; a projection or import whose `playerIdSpace` differs from the live pool is served without squaring, and every update carries the pool's space for db-service to refuse a mismatch — a flag flip alone added the whole Yahoo pool beside the ESPN rows on first open and the next save dropped the hidden ones for good; running the reverse only as a one-off script was rejected because the gate, the dry run and the collision rules would have to be copied, and gating on Yahoo's games was rejected because Yahoo's 2026 season lines are empty before the season starts.
[2026-09-15] fantasy-bff: a lone middle initial is dropped from a player's name key, so ESPN's "Elias N. Pettersson" keys as "Elias Pettersson" and the sweater separates him from the centre — the staging remap to Yahoo left his rows in 186 projections behind on his ESPN id; a manual `nhlId:platformId` override was rejected because it is per environment and per direction and the next initial would need another, and dropping every one-letter token was rejected because "J.T. Miller" would become "Miller".
[2026-09-15] fantasy-bff: the `admin` claim needs a verified email as well as an address in `ADMIN_EMAILS` — registration signs an unverified account straight in, so an allowlisted address with no account yet made whoever registered it first an admin, and the repos going public published both the rule and Alexander's addresses (as commit authors); keying admins on user id was passed over as a config change in both environments for the same guarantee, and refusing to sign unverified accounts in at all as reversing the soft gate the product chose. Accounts that existed before email verification were marked verified by db-service V8, so no current admin loses the role.
[2026-09-15] fantasy-bff: WireMock in tests runs with plain HTTP/2 off (`WireMockConfigs.http11()`), and the client tests use the JDK request factory production uses instead of `SimpleClientHttpRequestFactory` — the JDK `HttpClient` asks for h2c over `http://`, and on that path WireMock recorded every POST with an empty body and about one call in 250 reached its journal after the client already had its response, which failed `ResendPasswordResetEmailSenderTest` on CI (0 in 3,400 runs over HTTP/1.1); retrying or awaiting the verify was rejected because the bodies would still be empty, and pinning production clients to HTTP/1.1 because it changes production to suit a test double.
[2026-09-16] fantasy-bff: a projection started from the AI projection records `playerBasis=model`, and a player who joins the pool later gets the model's current line, or last season's where the model has none (every player without an NHL season) — before this the basis was left null and the reconciliation before the write stored `last_season`, so newcomers never saw the model; Alexander chose that a lapsed subscription keeps model lines for newcomers, so premium is not asked on reconcile, but a client-sent `model` basis is kept only with premium or a stored `model` basis, since otherwise any account could claim it and have every omitted row filled from the model. Existing AI projections stay `last_season` (they cannot be told apart reliably); reporting per newcomer whether the model or last season seeded it was passed over, since the notice would need a second persisted list for one sentence.
[2026-09-16] fantasy-bff: `GET /api/v1/espn/credentials/values` is removed with espn-service's endpoint behind it, so no BFF route returns a user's stored ESPN cookies (espn-service#31, web#676) — the only caller prefilled masked fields, and a readable session cookie turns any XSS or stolen SlapStat session into a stolen ESPN login.
[2026-09-16] fantasy-bff: `GET /api/v1/versions` stays public, with a 10-second shared cache in `VersionService` and a per-IP limit of 30 a minute, rather than closed behind a token — the promotion workflows poll production's copy unauthenticated and hold no token the BFF could check without a new secret in four repos, and the versions it reveals are already public as each repo's published GitHub releases, so closing it would cost a secret and hide nothing; the cost that was real is the four-service fan-out per request, which the cache ends.
[2026-09-16] fantasy-bff: verification and password-reset emails are capped per address (3 an hour each, `emails-per-address`) on top of the per-IP rules for `/verify/resend`, `/verify` and `/refresh` — a per-IP limit stops one caller but not many callers aiming at one inbox; the cap answers 200 and sends nothing, so it reveals no more about an account than the endpoints already do, and the password-reset path got it too because it sends mail to a caller-chosen address the same way.
[2026-09-16] fantasy-bff: "sign out everywhere" is `POST /api/v1/account/sessions/revoke`, which bumps the account's `tokenVersion` in db-service, and `/refresh` keeps issuing a new refresh token without invalidating the old one — true rotation (the old token refused once used, reuse detected) needs a record per issued token, and doing it with the one counter would sign out every other device and every other tab racing the same refresh on each refresh; a per-session token table was passed over as the first server-side session state in a stateless BFF, for a gain (revoking one device, catching a replayed token) the finding did not need. Access tokens stay valid for their 15 minutes after a revoke.
[2026-09-16] fantasy-bff: the container runs as `app` with a fixed UID/GID of 10001, given numerically to `USER`, instead of root — the base image's existing `ubuntu` user (1000) was rejected as an account the image's maintainers own and may change, and handing the user a writable `/app` was rejected because the service writes only to `java.io.tmpdir` (/tmp), so the unpacked jars stay root-owned and read-only to it.
[2026-09-17] fantasy-bff: Stripe replaces Paddle (whose live account was refused for good) as a new `StripePaymentProvider` beside Paddle's rather than in its place, because every environment still sets `PAYMENTS_PROVIDER=paddle` and a BFF with no bean for it would not start, while `mock` in prod would hand out Premium for free; it calls Stripe over `RestClient` with a pinned `Stripe-Version` and parses webhooks as JSON — the stripe-java library was declined because its event deserializer silently yields nothing when the webhook endpoint's API version differs from the library's; Managed Payments defaults on so a missing variable cannot make us the seller of record.
[2026-09-17] fantasy-bff: a draft's league is its own `DraftSettings` inside `DraftState`, mirrored from db-service, not a copy of `ProjectionSettings` (see fantasy-db-service DECISIONS, same day); the goalie minimum cap moves from 82 to 84 with it.
[2026-09-17] fantasy-bff: the Paddle provider, its signature verifier, configuration and tests are deleted now that staging and production both run Stripe — kept until then because a BFF whose PAYMENTS_PROVIDER names no bean does not start.
[2026-09-17] fantasy-bff: user feedback is filed as an issue in the private repo `pgaberra/slapstat-feedback`, carrying the reporter's email so the answer goes out by mail, and only signed-in users may send it (Alexander's call) — rejected: issues straight into the public service repos, since no scrubbing catches names, league links or pasted pages and it would let anyone publish under the account (an issue cannot be transferred from a private repo to a public one either, so a public issue is always a rewrite); GitHub's own issue forms, which need a GitHub account and are public at once; and mailing the report to info@ with Reply-To, which loses the open/closed tracking and the triage a later round can do on issues.
[2026-09-17] fantasy-bff: each filed feedback issue also mails its title and link to `feedback.notify-email` through Resend, because the issues are opened with Alexander's own token and GitHub never notifies an account of its own actions — rejected: a bot account or GitHub App as the issue author, which notifies natively but is one more identity and credential to keep; the mail carries neither the message nor the reporter's address, and a failed mail is logged, not thrown, since the report itself went through.
[2026-09-17] fantasy-bff: feedback is always served, with `FEEDBACK_ENABLED` and `feedback` on `/api/v1/features` removed and `FEEDBACK_GITHUB_TOKEN` required like the other secrets (Alexander's call: it does not need to be switchable) — rejected: keeping the flag defaulted to true, which leaves a field every client must read and a 404 path nobody takes; the dev profile stands in a placeholder token so a local run starts, and sending feedback there answers 502.
[2026-09-17] fantasy-bff: `POST /api/v1/projections` honours `kind: imported` and stores db-service's `IMPORTED` with the caller's own rows and no origin, so a spreadsheet import lands under Imports like a share-link copy (Alexander's call); `source` with it is refused (400), since an import is by definition rows the server did not choose — rejected: a separate spreadsheet-import endpoint, which would repeat the create path's reconciliation, basis settling and id-space stamping for the same write, and inventing a synthetic origin, since `origin` means "copied from this share link" and every reader already treats it as optional.
[2026-09-17] fantasy-bff: following a league's live draft is one answer, `LeagueDraftSyncAvailability` (`DRAFT_LEAGUE_SYNC_ENABLED`, off by default, AND a pool on Yahoo ids), enforced by a 404 on `GET /yahoo/leagues/{key}/draft` and reported as `leagueDraftSync` on `/api/v1/features` like the AI projection, and not premium (Alexander's call) — rejected: a web build flag like `ESPN_LEAGUES_ENABLED`, which leaves the endpoint open and needs a rebuild to flip; and polling Yahoo from the web through a pass-through of yahoo-service's model, since the board needs team ids and a gap-free pick list, which is the BFF's shape to own.
[2026-09-17] fantasy-bff: the streamer planner is switched by `STREAMER_PLANNER_ENABLED` read straight off `StreamerPlannerProperties`, with no availability bean, enforced by a 404 on `/api/v1/streamer-planner/**` and reported as `streamerPlanner` on `/api/v1/features` (fantasy-web#751) — an `XxxAvailability` like the draft sync's was rejected while there is nothing to combine the flag with, and a web build arg like Who's Hot's because it leaves the endpoints open and needs a rebuild to flip; the teams endpoint takes dates rather than a week number so the league's own matchup periods can use it unchanged.
[2026-09-17] fantasy-bff: the streamer planner's free agents are joined to the model's range projection on **identity** (`PlayerIdResolver` against the platform's own wire), and the switch moved into a `StreamerPlannerAvailability` bean now that two services enforce it (fantasy-web#751) — joining through the player pool's id space was rejected because a Yahoo league's free agents carry Yahoo ids and the pool may be ESPN-numbered, which would match nothing; scoring the players in the BFF was rejected because a ranking it computed could not be re-weighted by the league's own scoring settings, so the stats go out in the projection's vocabulary as the splits already do. This supersedes the same day's "no availability bean" line: the reason it gave (nothing to combine the flag with) still holds, but two callers enforcing one switch is its own reason.
[2026-09-17] fantasy-bff: the share page's author picture is a public endpoint of its own (`GET /api/v1/shared/{token}/avatar`) whose address the snapshot carries as `authorAvatar`, versioned by the picture's stamp, rather than bytes on the snapshot — a picture is the same for every reader of a link and belongs in the browser's cache, which a field on a response that is re-fetched on every sort and filter could never be; it is deliberately not behind `players.avatars.enabled`, which exists because the platform's player photographs are unlicensed, where an account's own picture is its own.
[2026-09-19] fantasy-bff: every error the advice writes is sent as `application/json` by an explicit `Content-Type`, not negotiated against `Accept` — the web asks for the profile picture as `image/*`, so no converter could write the ErrorDto, the handler itself failed, and the container's `/error` dispatch, which runs without the caller's JWT, answered 401: a db-service fault on the picture reached the web as a signed-out session instead of a 502. Rejected: permitting the ERROR dispatch in `SecurityConfig`, since Boot's error page cannot write JSON to `image/*` either and would answer 500 with no body, still losing the status and logging the fault twice; and having the web add `application/json` to its `Accept`, which fixes one caller and leaves every image and HTML endpoint open to the same fall-through.
[2026-09-19] fantasy-bff: the container's error dispatch is permitted in `SecurityConfig` and answered by our own `ErrorPageController` (the advice's ErrorDto, as JSON, with the status the error already has), and the catch-all answers a Spring MVC `ErrorResponse` 4xx with its own status — the error dispatch carries no token, so on a real Tomcat a fault in a filter and every Spring Security 403 reached the web as a 401, and a GET to a POST-only path, a 415, a 406 or a missing multipart part was a 500 with an ERROR log. Rejected: extending `ResponseEntityExceptionHandler`, which covers a fixed list, writes ProblemDetail and collides with the handlers already here; writing the ErrorDto straight from the security entry point and a front-most filter, which closes the two known routes to `/error` and leaves any other `sendError` to become a 401; and logging a filter fault on the error page, since Tomcat has already logged it at ERROR with its trace.
[2026-09-19] fantasy-bff: re-pinned projection-service's spec for `marcel-v81` (fantasy-projection-service#203) with no other change — the seed and projection fields keep their shape and only their values move from the finishing table to each player's expectation; the new `/finishing-table/{skaters,goalies}` leaderboard is generated into the client but not called or exposed, since nothing in the web reads a leaderboard yet; a model-based projection's newcomers added by `ProjectionPoolReconciler` are seeded from the model's current line, so a projection seeded before the switch gets expectation numbers for them beside its table-era rows, which was left as is (they are few, mostly players the model has no line for).
[2026-09-19] fantasy-bff: `/players/injuries` reads projection-service's ESPN `injury_*` fields again, reverting #283 with fantasy-projection-service#202/#205 - the merged `absence` it read no longer exists, injury handling being ESPN-only at Alexander's call; the pinned projection-service spec is *not* rolled back with it but re-pinned to that service's master, since the same pin also carries #204's rest-of-season endpoint, which stays.
[2026-09-19] fantasy-bff: a followed board is squared with the player pool in memory on every read and never written back, while copies, own projections and spreadsheet imports keep the write-back (fantasy-db-service#139) — db-service now takes only `data.draft` on a follow, so the save after an import stored none of the reconciled rows and would have had the read report rows the store did not hold; a follow's `unacknowledgedNewPlayerIds` is dropped rather than added to, because the follower cannot edit the board and the list arriving in the mirror is the author's own unread notice; the price is a pool read per open of a follow whose author's board is behind the latest sync, and none while he keeps his own board squared.
[2026-09-19] fantasy-bff: a relayed downstream 400/404/409/412 carries the downstream `ErrorDto`'s own `message`, falling back to the status's reason phrase, instead of `ex.getMessage()` — that embeds the whole raw response body inline, so db-service's new "this link is your own board" would have reached the web wrapped in JSON, and a proxy's HTML error page would have put markup in our message field; keeping the raw body as the fallback was rejected for the same reason.
[2026-09-20] fantasy-bff: starting a draft against a board is its own endpoint (`POST /api/v1/projections/{id}/drafts`) that has db-service copy the rows, rather than the client POSTing a copy through `POST /projections` — the rows are ~0.5 MB the client would download and upload back, which is the upload that was already failing in production (JAVA-SPRING-BOOT-J); a `sourceProjectionId` on the create request was rejected because create is also the preset path and would then mean two different things, and the kind `preset_draft` is gone since a draft against a preset is just a draft whose `preset` is set. Nothing is reconciled against the pool on the copy: a draft is a snapshot, and adding players mid-draft would move the numbers under whoever is picking. `PUT /api/v1/projections/{id}/name` renames a board or a draft without sending the board, with `derived: true` for a name the app worked out (a league sync), which db-service numbers on a clash and declines once the user has named the row.
[2026-09-20] fantasy-bff: a draft against a preset is named by the caller like everything else, instead of being overruled with the preset's name — the override existed so a draft could not claim to have been drafted against something it wasn't, and that stopped meaning anything when a draft became a row its owner can rename on sight; provenance lives in `preset`/`sourceProjectionId`, which the app reads and shows. Keeping the override and adding a separate "name this draft" call was rejected as two ways to set one field, and db-service still numbers a name the user already holds.

[2026-09-21] fantasy-bff: `POST /api/v1/projections/copies` copies only, following db-service#143 — calling `/imports` after it to keep the old "a copy also leaves you following the link" behaviour was rejected because it puts two unatomic writes behind one button and a failure between them leaves the copy without the follow anyway; a user who wants both presses both, and the "this link is your own board" 400 now lives on `/imports` alone.
[2026-09-22] fantasy-bff: how a board is scored is ported here (`service/scoring/`) rather than left to the browser — a league summary shown to an account without premium has to be totalled where the lines can be withheld, and a total the browser computed is a total it was given the lines for; the port is held to the web's own output cell for cell by golden vectors pinned at `src/test/resources/scoring/scoring-vectors.json` (generated by fantasy-web's `scoring-vectors.spec.ts`, re-pinned like a downstream spec), and `JsNumbers` reproduces `toFixed` to the bit because a board is scored on the numbers as they are written; rejected: scoring in the projection service (it owns the model, not the leagues' rules), and a looser tolerance in the vectors, which would hide the drift the file exists to catch.
[2026-09-22] fantasy-bff: the league summary endpoint takes a **league key**, not the picks — `GET /api/v1/league-summaries/yahoo/{leagueKey}` reads the teams, the picks and the scoring settings from Yahoo itself, so nothing about the rosters is the caller's to choose; a version taking client-supplied picks was rejected because the free half of the feature (aggregates without the lines) would then be a per-player readout of the model: a "league" of one-player teams makes every team total one player's value, and differencing two rosters does the rest. The same read also gates it behind `DRAFT_LEAGUE_SYNC_ENABLED` and a Yahoo-numbered pool for free, since `YahooLeagueDraftService` already enforces both. Premium is decided here and not in the client: without it the per-player halves are dropped from the response (`LeagueSummary.aggregatesOnly()`) rather than hidden by the page.
[2026-09-23] fantasy-bff: a new endpoint's security rule is enforced by a test that walks the mappings (`EveryEndpointIsReachableTest`), not by remembering to add one — `anyRequest().denyAll()` is the right default and also a trap, and it let the league summary ship written, specced, tested and deployed while answering 403 to its own page; the test performs every mapped GET as a signed-in admin against stubbed downstreams and fails on a 403, with the model's own kill switch turned on so that a feature denied on purpose is not read as one denied by omission; rejected: naming each path in a list (the same memory that failed the first time) and asserting on the config's matchers by reflection (brittle, and it would not have caught a path the config covers but the dispatcher maps differently).
[2026-09-23] fantasy-bff: a db-service read the ordinary client cancelled at three seconds is asked once more (`HttpDatabaseServiceClient.retryingRead`, on `listProjections` and `findUserByEmail`) instead of the timeout being changed — the two candidates #299 named were both worse: moving the summary list onto the 15 s projection client reverses #178 on purpose and sizes a names-and-dates call for half a megabyte of jsonb, and raising `services.database.timeout-ms` slows every db call's failure, login included, to a number nobody has measured. Two attempts at three seconds still give up sooner than one at fifteen, so fail-fast survives. Retried narrowly and on transport failure only, found by an `IOException` in the cause chain rather than by exception type, because the one fault arrives as two shapes — `ResourceAccessException` when the cancellation precedes the read (JAVA-SPRING-BOOT-1C, 2J) and a plain `RestClientException` out of deserialisation when it lands mid-parse (2Q, where the array had already been bound and only the read-ahead for end-of-input was left). A `ClientHttpRequestInterceptor` was rejected for exactly that second shape: the interceptor has returned its response before the body fails, so it would have covered two of the three. A status db-service chose to send is never retried, 404 included; the retry logs at WARN so the slow moments stay countable rather than becoming invisible, and what made db-service slow — a cold start after a rollout is the unchecked candidate — is not addressed here and stays open on #299.
[2026-09-24] fantasy-bff: `DraftState.following` (whether a draft follows its linked league's live draft, absent = not following) is mapped both ways unchanged and stripped nowhere here — db-service decides where a draft goes (a copy, a follow and a share carry none, `startDraft` takes the caller's setup), so a BFF-side filter would be business logic in the BFF; deriving the flag from the linked league was rejected because it would start syncing a mock whose board only imported that league's settings, and browser storage because it remembers the switch on one device only.
[2026-09-24] fantasy-bff: the owner is mailed each new account as it happens, with the user's address and sign-up method (password, Google, Facebook), and no count — Alexander's call over a daily digest. A social sign-up is told from a sign-in by db-service's 201-vs-200 on `POST /users/google|facebook` (db-service#146), carried out as `DatabaseServiceClient.ResolvedUser` the way `followShare` carries its status; a `created` field on the shared `UserResponse` was rejected. The mail goes out on its own virtual thread (the `DownstreamKeyVerifier` pattern, no executor bean), so sign-up latency never includes Resend, and every failure is logged at ERROR inside the task and never thrown.
[2026-09-24] fantasy-bff: `SIGNUP_NOTIFY_EMAIL` is optional (`${SIGNUP_NOTIFY_EMAIL:}`): unset or blank turns sign-up mails off with one WARN at startup, instead of a required property that stops startup — a merge deploys staging at once and the variable is set by hand in Coolify, so a required one would have taken staging down until then; it is an address, not a secret, so the no-default rule for secrets does not apply, and no literal address was committed (unlike `FEEDBACK_NOTIFY_EMAIL`'s default).
